# 全局 UI 规范

## 布局
- 移动端页面统一使用 `app-container`。
- 主内容区优先采用卡片化结构。

## 视觉
- 主色系：teal / indigo（Tailwind）。
- 卡片：白底 + 细边框 + 圆角。
- 文本层级：标题 > 正文 > 辅助信息。

## 交互
- 可点击区域提供 hover/active 反馈。
- 空状态统一使用简洁说明文案。
- 弹窗和抽屉交互尽量复用已有样式。
